import { RelevanceDirective } from './relevance.directive';

describe('RelevanceDirective', () => {
  it('should create an instance', () => {
    const directive = new RelevanceDirective();
    expect(directive).toBeTruthy();
  });
});
